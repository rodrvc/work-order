<?xml version="1.0" encoding="utf-8"?>



<com.google.android.material.card.MaterialCardView xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/card"
    style="@style/Widget.MaterialComponents.CardView"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginLeft="@dimen/mtrl_card_spacing"
    android:layout_marginTop="@dimen/mtrl_card_spacing"
    android:layout_marginRight="@dimen/mtrl_card_spacing"
    android:minHeight="100dp"
    app:cardCornerRadius="2dp"
    android:clickable="true"

    app:rippleColor="@color/colorPrimary"
    app:checkedIcon="@color/cardview_dark_background"
    android:background="?android:selectableItemBackground"
    android:foreground="?attr/actionBarDivider"
    android:touchscreenBlocksFocus="true"


    >


    <TextView
        android:id="@+id/card_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/card_text"
        android:layout_gravity="left"
        android:padding="10dp"
        android:textSize="@dimen/text_card_dimen"
        android:textStyle="bold"
        android:layout_marginLeft="5dp"
        />

    <TextView
        android:id="@+id/card_description"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/card_description"
        android:layout_gravity="left"
        android:layout_marginTop="50dp"
        android:textSize="@dimen/text_card_dimen_description"
        android:layout_marginLeft="15dp"
        />


</com.google.android.material.card.MaterialCardView>
